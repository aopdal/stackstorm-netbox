# This action was auto generated from the NetBox API Swagger Spec
# NetBox API version: 4.6
description: "Read-only list of ObjectTypes."
enabled: true
entry_point: run.py
name: get.core.object_types
parameters:
  endpoint_uri:
    default: "/core/object-types/"
    immutable: true
    type: string
  http_verb:
    default: get
    immutable: true
    type: string
  get_detail_route_eligible:
    default: true
    immutable: true
    type: boolean
  fail_non_2xx:
    default: false
    type: boolean
    description: Set action as failed when http return reponse status isn't 2xx.
  app_label:
    required: false
    type: array
    description: "App_label"
  app_label__empty:
    required: false
    type: boolean
    description: "App_label is empty/null (boolean)"
  app_label__ic:
    required: false
    type: array
    description: "App_label contains (case-insensitive)"
  app_label__ie:
    required: false
    type: array
    description: "App_label exact match (case-insensitive)"
  app_label__iew:
    required: false
    type: array
    description: "App_label ends with (case-insensitive)"
  app_label__iregex:
    required: false
    type: array
    description: "App_label"
  app_label__isw:
    required: false
    type: array
    description: "App_label starts with (case-sensitive)"
  app_label__n:
    required: false
    type: array
    description: "App_label not equal to"
  app_label__nic:
    required: false
    type: array
    description: "App_label does not contain (case-insensitive)"
  app_label__nie:
    required: false
    type: array
    description: "App_label inverse exact match (case-insensitive)"
  app_label__niew:
    required: false
    type: array
    description: "App_label does not end with (case-insensitive)"
  app_label__nisw:
    required: false
    type: array
    description: "App_label does not start with (case-sensitive)"
  app_label__regex:
    required: false
    type: array
    description: "App_label"
  brief:
    required: false
    type: boolean
    description: "Return only brief fields for each object."
  features:
    required: false
    type: string
    description: "Features"
  fields:
    required: false
    type: string
    description: "Comma-separated list of fields to include in the response. Example: `fields=id,name`."
  id:
    required: false
    type: array
    description: "Id"
  id__empty:
    required: false
    type: boolean
    description: "Id is empty/null (boolean)"
  id__gt:
    required: false
    type: array
    description: "Id greater than"
  id__gte:
    required: false
    type: array
    description: "Id greater than or equal to"
  id__lt:
    required: false
    type: array
    description: "Id less than"
  id__lte:
    required: false
    type: array
    description: "Id less than or equal to"
  id__n:
    required: false
    type: array
    description: "Id not equal to"
  limit:
    required: false
    type: integer
    description: "Number of results to return per page."
  model:
    required: false
    type: array
    description: "Model"
  model__empty:
    required: false
    type: boolean
    description: "Model is empty/null (boolean)"
  model__ic:
    required: false
    type: array
    description: "Model contains (case-insensitive)"
  model__ie:
    required: false
    type: array
    description: "Model exact match (case-insensitive)"
  model__iew:
    required: false
    type: array
    description: "Model ends with (case-insensitive)"
  model__iregex:
    required: false
    type: array
    description: "Model"
  model__isw:
    required: false
    type: array
    description: "Model starts with (case-sensitive)"
  model__n:
    required: false
    type: array
    description: "Model not equal to"
  model__nic:
    required: false
    type: array
    description: "Model does not contain (case-insensitive)"
  model__nie:
    required: false
    type: array
    description: "Model inverse exact match (case-insensitive)"
  model__niew:
    required: false
    type: array
    description: "Model does not end with (case-insensitive)"
  model__nisw:
    required: false
    type: array
    description: "Model does not start with (case-sensitive)"
  model__regex:
    required: false
    type: array
    description: "Model"
  offset:
    required: false
    type: integer
    description: "The initial index from which to return the results."
  omit:
    required: false
    type: string
    description: "Comma-separated list of fields to exclude from the response. Example: `omit=description,tags`."
  ordering:
    required: false
    type: string
    description: "Which field to use when ordering the results."
  public:
    required: false
    type: boolean
    description: "Public"
  q:
    required: false
    type: string
    description: "Search"
  start:
    required: false
    type: integer
    description: "Cursor-based pagination: return results with pk &gt;= start, ordered by pk. Mutually exclusive with offset."
  save_in_key_store:
    type: boolean
    default: false
    description: Save the result of the action as a json object in the st2 key store. Used when the expected result from Netbox is very large and the result will be piped to another action. You must also specify a save_in_key_store_keyname and an optional save_in_key_store_ttl.
  save_in_key_store_key_name:
    type: string
    description: Name of the key to store the json result value in the st2 key store. Must be used with save_in_key_store and optionally save_in_key_store_ttl.
  save_in_key_store_ttl:
    type: integer
    default: 60
    description: TTL (seconds) of the saved json result in the st2 key store. Defaults to 60 seconds. Must be used with save_in_key_store and save_in_key_store_key_name.
runner_type: python-script
