# This action was auto generated from the NetBox API Swagger Spec
# NetBox API version: 4.6
description: "Retrieve a list of job results"
enabled: true
entry_point: run.py
name: get.core.jobs
parameters:
  endpoint_uri:
    default: "/core/jobs/"
    immutable: true
    type: string
  http_verb:
    default: get
    immutable: true
    type: string
  get_detail_route_eligible:
    default: true
    immutable: true
    type: boolean
  fail_non_2xx:
    default: false
    type: boolean
    description: Set action as failed when http return reponse status isn't 2xx.
  brief:
    required: false
    type: boolean
    description: "Return only brief fields for each object."
  completed:
    required: false
    type: string
    description: "Completed"
  completed__after:
    required: false
    type: string
    description: "Completed"
  completed__before:
    required: false
    type: string
    description: "Completed"
  created:
    required: false
    type: string
    description: "Created"
  created__after:
    required: false
    type: string
    description: "Created"
  created__before:
    required: false
    type: string
    description: "Created"
  fields:
    required: false
    type: string
    description: "Comma-separated list of fields to include in the response. Example: `fields=id,name`."
  id:
    required: false
    type: array
    description: "Id"
  id__empty:
    required: false
    type: boolean
    description: "Id is empty/null (boolean)"
  id__gt:
    required: false
    type: array
    description: "Id greater than"
  id__gte:
    required: false
    type: array
    description: "Id greater than or equal to"
  id__lt:
    required: false
    type: array
    description: "Id less than"
  id__lte:
    required: false
    type: array
    description: "Id less than or equal to"
  id__n:
    required: false
    type: array
    description: "Id not equal to"
  interval:
    required: false
    type: array
    description: "Interval"
  interval__empty:
    required: false
    type: boolean
    description: "Interval is empty/null (boolean)"
  interval__gt:
    required: false
    type: array
    description: "Interval greater than"
  interval__gte:
    required: false
    type: array
    description: "Interval greater than or equal to"
  interval__lt:
    required: false
    type: array
    description: "Interval less than"
  interval__lte:
    required: false
    type: array
    description: "Interval less than or equal to"
  interval__n:
    required: false
    type: array
    description: "Interval not equal to"
  job_id:
    required: false
    type: string
    description: "Job_id"
  limit:
    required: false
    type: integer
    description: "Number of results to return per page."
  name:
    required: false
    type: array
    description: "Name"
  name__empty:
    required: false
    type: boolean
    description: "Name is empty/null (boolean)"
  name__ic:
    required: false
    type: array
    description: "Name contains (case-insensitive)"
  name__ie:
    required: false
    type: array
    description: "Name exact match (case-insensitive)"
  name__iew:
    required: false
    type: array
    description: "Name ends with (case-insensitive)"
  name__iregex:
    required: false
    type: array
    description: "Name"
  name__isw:
    required: false
    type: array
    description: "Name starts with (case-sensitive)"
  name__n:
    required: false
    type: array
    description: "Name not equal to"
  name__nic:
    required: false
    type: array
    description: "Name does not contain (case-insensitive)"
  name__nie:
    required: false
    type: array
    description: "Name inverse exact match (case-insensitive)"
  name__niew:
    required: false
    type: array
    description: "Name does not end with (case-insensitive)"
  name__nisw:
    required: false
    type: array
    description: "Name does not start with (case-sensitive)"
  name__regex:
    required: false
    type: array
    description: "Name"
  notifications:
    required: false
    type: array
    description: "Notifications"
  notifications__empty:
    required: false
    type: boolean
    description: "Notifications is empty/null (boolean)"
  notifications__ic:
    required: false
    type: array
    description: "Notifications contains (case-insensitive)"
  notifications__ie:
    required: false
    type: array
    description: "Notifications exact match (case-insensitive)"
  notifications__iew:
    required: false
    type: array
    description: "Notifications ends with (case-insensitive)"
  notifications__iregex:
    required: false
    type: array
    description: "Notifications"
  notifications__isw:
    required: false
    type: array
    description: "Notifications starts with (case-sensitive)"
  notifications__n:
    required: false
    type: array
    description: "Notifications not equal to"
  notifications__nic:
    required: false
    type: array
    description: "Notifications does not contain (case-insensitive)"
  notifications__nie:
    required: false
    type: array
    description: "Notifications inverse exact match (case-insensitive)"
  notifications__niew:
    required: false
    type: array
    description: "Notifications does not end with (case-insensitive)"
  notifications__nisw:
    required: false
    type: array
    description: "Notifications does not start with (case-sensitive)"
  notifications__regex:
    required: false
    type: array
    description: "Notifications"
  object_id:
    required: false
    type: array
    description: "Object_id"
  object_id__empty:
    required: false
    type: boolean
    description: "Object_id is empty/null (boolean)"
  object_id__gt:
    required: false
    type: array
    description: "Object_id greater than"
  object_id__gte:
    required: false
    type: array
    description: "Object_id greater than or equal to"
  object_id__lt:
    required: false
    type: array
    description: "Object_id less than"
  object_id__lte:
    required: false
    type: array
    description: "Object_id less than or equal to"
  object_id__n:
    required: false
    type: array
    description: "Object_id not equal to"
  object_type:
    required: false
    type: array
    description: "Object_type"
  object_type__n:
    required: false
    type: array
    description: "Object_type not equal to"
  object_type_id:
    required: false
    type: array
    description: "Object_type_id"
  object_type_id__n:
    required: false
    type: array
    description: "Object_type_id not equal to"
  offset:
    required: false
    type: integer
    description: "The initial index from which to return the results."
  omit:
    required: false
    type: string
    description: "Comma-separated list of fields to exclude from the response. Example: `omit=description,tags`."
  ordering:
    required: false
    type: string
    description: "Which field to use when ordering the results."
  q:
    required: false
    type: string
    description: "Search"
  queue_name:
    required: false
    type: string
    description: "Queue_name"
  queue_name__empty:
    required: false
    type: boolean
    description: "Queue_name is empty/null (boolean)"
  queue_name__ic:
    required: false
    type: string
    description: "Queue_name contains (case-insensitive)"
  queue_name__ie:
    required: false
    type: string
    description: "Queue_name exact match (case-insensitive)"
  queue_name__iew:
    required: false
    type: string
    description: "Queue_name ends with (case-insensitive)"
  queue_name__iregex:
    required: false
    type: string
    description: "Queue_name"
  queue_name__isw:
    required: false
    type: string
    description: "Queue_name starts with (case-sensitive)"
  queue_name__n:
    required: false
    type: string
    description: "Queue_name not equal to"
  queue_name__nic:
    required: false
    type: string
    description: "Queue_name does not contain (case-insensitive)"
  queue_name__nie:
    required: false
    type: string
    description: "Queue_name inverse exact match (case-insensitive)"
  queue_name__niew:
    required: false
    type: string
    description: "Queue_name does not end with (case-insensitive)"
  queue_name__nisw:
    required: false
    type: string
    description: "Queue_name does not start with (case-sensitive)"
  queue_name__regex:
    required: false
    type: string
    description: "Queue_name"
  scheduled:
    required: false
    type: string
    description: "Scheduled"
  scheduled__after:
    required: false
    type: string
    description: "Scheduled"
  scheduled__before:
    required: false
    type: string
    description: "Scheduled"
  start:
    required: false
    type: integer
    description: "Cursor-based pagination: return results with pk &gt;= start, ordered by pk. Mutually exclusive with offset."
  started:
    required: false
    type: string
    description: "Started"
  started__after:
    required: false
    type: string
    description: "Started"
  started__before:
    required: false
    type: string
    description: "Started"
  status:
    required: false
    type: array
    description: "Status"
  status__empty:
    required: false
    type: boolean
    description: "Status is empty/null (boolean)"
  status__ic:
    required: false
    type: array
    description: "Status contains (case-insensitive)"
  status__ie:
    required: false
    type: array
    description: "Status exact match (case-insensitive)"
  status__iew:
    required: false
    type: array
    description: "Status ends with (case-insensitive)"
  status__iregex:
    required: false
    type: array
    description: "Status"
  status__isw:
    required: false
    type: array
    description: "Status starts with (case-sensitive)"
  status__n:
    required: false
    type: array
    description: "Status not equal to"
  status__nic:
    required: false
    type: array
    description: "Status does not contain (case-insensitive)"
  status__nie:
    required: false
    type: array
    description: "Status inverse exact match (case-insensitive)"
  status__niew:
    required: false
    type: array
    description: "Status does not end with (case-insensitive)"
  status__nisw:
    required: false
    type: array
    description: "Status does not start with (case-sensitive)"
  status__regex:
    required: false
    type: array
    description: "Status"
  user:
    required: false
    type: array
    description: "User name"
  user__n:
    required: false
    type: array
    description: "User not equal to"
  user_id:
    required: false
    type: array
    description: "User (ID)"
  user_id__n:
    required: false
    type: array
    description: "User_id not equal to"
  save_in_key_store:
    type: boolean
    default: false
    description: Save the result of the action as a json object in the st2 key store. Used when the expected result from Netbox is very large and the result will be piped to another action. You must also specify a save_in_key_store_keyname and an optional save_in_key_store_ttl.
  save_in_key_store_key_name:
    type: string
    description: Name of the key to store the json result value in the st2 key store. Must be used with save_in_key_store and optionally save_in_key_store_ttl.
  save_in_key_store_ttl:
    type: integer
    default: 60
    description: TTL (seconds) of the saved json result in the st2 key store. Defaults to 60 seconds. Must be used with save_in_key_store and save_in_key_store_key_name.
runner_type: python-script
