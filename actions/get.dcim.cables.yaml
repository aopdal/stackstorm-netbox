# This action was auto generated from the NetBox API Swagger Spec
# NetBox API version: 4.5
description: "Get a list of cable objects."
enabled: true
entry_point: run.py
name: get.dcim.cables
parameters:
  endpoint_uri:
    default: "/dcim/cables/"
    immutable: true
    type: string
  http_verb:
    default: get
    immutable: true
    type: string
  get_detail_route_eligible:
    default: true
    immutable: true
    type: boolean
  fail_non_2xx:
    default: false
    type: boolean
    description: Set action as failed when http return reponse status isn't 2xx.
  circuittermination_id:
    required: false
    type: array
    description: "Circuittermination_id"
  color:
    required: false
    type: array
    description: "Color"
  color__empty:
    required: false
    type: boolean
    description: "Color is empty/null (boolean)"
  color__ic:
    required: false
    type: array
    description: "Color contains (case-insensitive)"
  color__ie:
    required: false
    type: array
    description: "Color exact match (case-insensitive)"
  color__iew:
    required: false
    type: array
    description: "Color ends with (case-insensitive)"
  color__iregex:
    required: false
    type: array
    description: "Color"
  color__isw:
    required: false
    type: array
    description: "Color starts with (case-sensitive)"
  color__n:
    required: false
    type: array
    description: "Color not equal to"
  color__nic:
    required: false
    type: array
    description: "Color does not contain (case-insensitive)"
  color__nie:
    required: false
    type: array
    description: "Color inverse exact match (case-insensitive)"
  color__niew:
    required: false
    type: array
    description: "Color does not end with (case-insensitive)"
  color__nisw:
    required: false
    type: array
    description: "Color does not start with (case-sensitive)"
  color__regex:
    required: false
    type: array
    description: "Color"
  consoleport_id:
    required: false
    type: array
    description: "Consoleport_id"
  consoleserverport_id:
    required: false
    type: array
    description: "Consoleserverport_id"
  created:
    required: false
    type: array
    description: "Created"
  created__empty:
    required: false
    type: array
    description: "Created is empty/null (boolean)"
  created__gt:
    required: false
    type: array
    description: "Created greater than"
  created__gte:
    required: false
    type: array
    description: "Created greater than or equal to"
  created__lt:
    required: false
    type: array
    description: "Created less than"
  created__lte:
    required: false
    type: array
    description: "Created less than or equal to"
  created__n:
    required: false
    type: array
    description: "Created not equal to"
  created_by_request:
    required: false
    type: string
    description: "Created_by_request"
  description:
    required: false
    type: array
    description: "Description"
  description__empty:
    required: false
    type: boolean
    description: "Description is empty/null (boolean)"
  description__ic:
    required: false
    type: array
    description: "Description contains (case-insensitive)"
  description__ie:
    required: false
    type: array
    description: "Description exact match (case-insensitive)"
  description__iew:
    required: false
    type: array
    description: "Description ends with (case-insensitive)"
  description__iregex:
    required: false
    type: array
    description: "Description"
  description__isw:
    required: false
    type: array
    description: "Description starts with (case-sensitive)"
  description__n:
    required: false
    type: array
    description: "Description not equal to"
  description__nic:
    required: false
    type: array
    description: "Description does not contain (case-insensitive)"
  description__nie:
    required: false
    type: array
    description: "Description inverse exact match (case-insensitive)"
  description__niew:
    required: false
    type: array
    description: "Description does not end with (case-insensitive)"
  description__nisw:
    required: false
    type: array
    description: "Description does not start with (case-sensitive)"
  description__regex:
    required: false
    type: array
    description: "Description"
  device:
    required: false
    type: array
    description: "Device"
  device_id:
    required: false
    type: array
    description: "Device_id"
  frontport_id:
    required: false
    type: array
    description: "Frontport_id"
  id:
    required: false
    type: array
    description: "Id"
  id__empty:
    required: false
    type: boolean
    description: "Id is empty/null (boolean)"
  id__gt:
    required: false
    type: array
    description: "Id greater than"
  id__gte:
    required: false
    type: array
    description: "Id greater than or equal to"
  id__lt:
    required: false
    type: array
    description: "Id less than"
  id__lte:
    required: false
    type: array
    description: "Id less than or equal to"
  id__n:
    required: false
    type: array
    description: "Id not equal to"
  interface_id:
    required: false
    type: array
    description: "Interface_id"
  label:
    required: false
    type: array
    description: "Label"
  label__empty:
    required: false
    type: boolean
    description: "Label is empty/null (boolean)"
  label__ic:
    required: false
    type: array
    description: "Label contains (case-insensitive)"
  label__ie:
    required: false
    type: array
    description: "Label exact match (case-insensitive)"
  label__iew:
    required: false
    type: array
    description: "Label ends with (case-insensitive)"
  label__iregex:
    required: false
    type: array
    description: "Label"
  label__isw:
    required: false
    type: array
    description: "Label starts with (case-sensitive)"
  label__n:
    required: false
    type: array
    description: "Label not equal to"
  label__nic:
    required: false
    type: array
    description: "Label does not contain (case-insensitive)"
  label__nie:
    required: false
    type: array
    description: "Label inverse exact match (case-insensitive)"
  label__niew:
    required: false
    type: array
    description: "Label does not end with (case-insensitive)"
  label__nisw:
    required: false
    type: array
    description: "Label does not start with (case-sensitive)"
  label__regex:
    required: false
    type: array
    description: "Label"
  last_updated:
    required: false
    type: array
    description: "Last_updated"
  last_updated__empty:
    required: false
    type: array
    description: "Last_updated is empty/null (boolean)"
  last_updated__gt:
    required: false
    type: array
    description: "Last_updated greater than"
  last_updated__gte:
    required: false
    type: array
    description: "Last_updated greater than or equal to"
  last_updated__lt:
    required: false
    type: array
    description: "Last_updated less than"
  last_updated__lte:
    required: false
    type: array
    description: "Last_updated less than or equal to"
  last_updated__n:
    required: false
    type: array
    description: "Last_updated not equal to"
  length:
    required: false
    type: array
    description: "Length"
  length__empty:
    required: false
    type: boolean
    description: "Length is empty/null (boolean)"
  length__gt:
    required: false
    type: array
    description: "Length greater than"
  length__gte:
    required: false
    type: array
    description: "Length greater than or equal to"
  length__lt:
    required: false
    type: array
    description: "Length less than"
  length__lte:
    required: false
    type: array
    description: "Length less than or equal to"
  length__n:
    required: false
    type: array
    description: "Length not equal to"
  length_unit:
    required: false
    type: string
    description: "* `km` - Kilometers
* `m` - Meters
* `cm` - Centimeters
* `mi` - Miles
* `ft` - Feet
* `in` - Inches"
  length_unit__empty:
    required: false
    type: boolean
    description: "Length_unit is empty/null (boolean)"
  length_unit__ic:
    required: false
    type: array
    description: "Length_unit contains (case-insensitive)"
  length_unit__ie:
    required: false
    type: array
    description: "Length_unit exact match (case-insensitive)"
  length_unit__iew:
    required: false
    type: array
    description: "Length_unit ends with (case-insensitive)"
  length_unit__iregex:
    required: false
    type: array
    description: "Length_unit"
  length_unit__isw:
    required: false
    type: array
    description: "Length_unit starts with (case-sensitive)"
  length_unit__n:
    required: false
    type: string
    description: "Length_unit not equal to"
  length_unit__nic:
    required: false
    type: array
    description: "Length_unit does not contain (case-insensitive)"
  length_unit__nie:
    required: false
    type: array
    description: "Length_unit inverse exact match (case-insensitive)"
  length_unit__niew:
    required: false
    type: array
    description: "Length_unit does not end with (case-insensitive)"
  length_unit__nisw:
    required: false
    type: array
    description: "Length_unit does not start with (case-sensitive)"
  length_unit__regex:
    required: false
    type: array
    description: "Length_unit"
  limit:
    required: false
    type: integer
    description: "Number of results to return per page."
  location:
    required: false
    type: array
    description: "Location"
  location_id:
    required: false
    type: array
    description: "Location_id"
  modified_by_request:
    required: false
    type: string
    description: "Modified_by_request"
  offset:
    required: false
    type: integer
    description: "The initial index from which to return the results."
  ordering:
    required: false
    type: string
    description: "Which field to use when ordering the results."
  owner:
    required: false
    type: array
    description: "Owner (name)"
  owner__n:
    required: false
    type: array
    description: "Owner not equal to"
  owner_group:
    required: false
    type: array
    description: "Owner Group (name)"
  owner_group__n:
    required: false
    type: array
    description: "Owner_group not equal to"
  owner_group_id:
    required: false
    type: array
    description: "Owner Group (ID)"
  owner_group_id__n:
    required: false
    type: array
    description: "Owner_group_id not equal to"
  owner_id:
    required: false
    type: array
    description: "Owner (ID)"
  owner_id__n:
    required: false
    type: array
    description: "Owner_id not equal to"
  powerfeed_id:
    required: false
    type: array
    description: "Powerfeed_id"
  poweroutlet_id:
    required: false
    type: array
    description: "Poweroutlet_id"
  powerport_id:
    required: false
    type: array
    description: "Powerport_id"
  profile:
    required: false
    type: array
    description: "Profile"
  profile__empty:
    required: false
    type: boolean
    description: "Profile is empty/null (boolean)"
  profile__ic:
    required: false
    type: array
    description: "Profile contains (case-insensitive)"
  profile__ie:
    required: false
    type: array
    description: "Profile exact match (case-insensitive)"
  profile__iew:
    required: false
    type: array
    description: "Profile ends with (case-insensitive)"
  profile__iregex:
    required: false
    type: array
    description: "Profile"
  profile__isw:
    required: false
    type: array
    description: "Profile starts with (case-sensitive)"
  profile__n:
    required: false
    type: array
    description: "Profile not equal to"
  profile__nic:
    required: false
    type: array
    description: "Profile does not contain (case-insensitive)"
  profile__nie:
    required: false
    type: array
    description: "Profile inverse exact match (case-insensitive)"
  profile__niew:
    required: false
    type: array
    description: "Profile does not end with (case-insensitive)"
  profile__nisw:
    required: false
    type: array
    description: "Profile does not start with (case-sensitive)"
  profile__regex:
    required: false
    type: array
    description: "Profile"
  q:
    required: false
    type: string
    description: "Search"
  rack:
    required: false
    type: array
    description: "Rack"
  rack_id:
    required: false
    type: array
    description: "Rack_id"
  rearport_id:
    required: false
    type: array
    description: "Rearport_id"
  site:
    required: false
    type: array
    description: "Site"
  site_id:
    required: false
    type: array
    description: "Site_id"
  status:
    required: false
    type: array
    description: "Status"
  status__empty:
    required: false
    type: boolean
    description: "Status is empty/null (boolean)"
  status__ic:
    required: false
    type: array
    description: "Status contains (case-insensitive)"
  status__ie:
    required: false
    type: array
    description: "Status exact match (case-insensitive)"
  status__iew:
    required: false
    type: array
    description: "Status ends with (case-insensitive)"
  status__iregex:
    required: false
    type: array
    description: "Status"
  status__isw:
    required: false
    type: array
    description: "Status starts with (case-sensitive)"
  status__n:
    required: false
    type: array
    description: "Status not equal to"
  status__nic:
    required: false
    type: array
    description: "Status does not contain (case-insensitive)"
  status__nie:
    required: false
    type: array
    description: "Status inverse exact match (case-insensitive)"
  status__niew:
    required: false
    type: array
    description: "Status does not end with (case-insensitive)"
  status__nisw:
    required: false
    type: array
    description: "Status does not start with (case-sensitive)"
  status__regex:
    required: false
    type: array
    description: "Status"
  tag:
    required: false
    type: array
    description: "Tag"
  tag__n:
    required: false
    type: array
    description: "Tag not equal to"
  tag_id:
    required: false
    type: array
    description: "Tag_id"
  tag_id__n:
    required: false
    type: array
    description: "Tag_id not equal to"
  tenant:
    required: false
    type: array
    description: "Tenant (slug)"
  tenant__n:
    required: false
    type: array
    description: "Tenant not equal to"
  tenant_group:
    required: false
    type: array
    description: "Tenant_group"
  tenant_group__n:
    required: false
    type: array
    description: "Tenant_group not equal to"
  tenant_group_id:
    required: false
    type: array
    description: "Tenant_group_id"
  tenant_group_id__n:
    required: false
    type: array
    description: "Tenant_group_id not equal to"
  tenant_id:
    required: false
    type: array
    description: "Tenant (ID)"
  tenant_id__n:
    required: false
    type: array
    description: "Tenant_id not equal to"
  termination_a_id:
    required: false
    type: array
    description: "Termination_a_id"
  termination_a_type:
    required: false
    type: array
    description: "Termination_a_type"
  termination_a_type__n:
    required: false
    type: array
    description: "Termination_a_type not equal to"
  termination_b_id:
    required: false
    type: array
    description: "Termination_b_id"
  termination_b_type:
    required: false
    type: array
    description: "Termination_b_type"
  termination_b_type__n:
    required: false
    type: array
    description: "Termination_b_type not equal to"
  type:
    required: false
    type: array
    description: "Type"
  type__empty:
    required: false
    type: boolean
    description: "Type is empty/null (boolean)"
  type__ic:
    required: false
    type: array
    description: "Type contains (case-insensitive)"
  type__ie:
    required: false
    type: array
    description: "Type exact match (case-insensitive)"
  type__iew:
    required: false
    type: array
    description: "Type ends with (case-insensitive)"
  type__iregex:
    required: false
    type: array
    description: "Type"
  type__isw:
    required: false
    type: array
    description: "Type starts with (case-sensitive)"
  type__n:
    required: false
    type: array
    description: "Type not equal to"
  type__nic:
    required: false
    type: array
    description: "Type does not contain (case-insensitive)"
  type__nie:
    required: false
    type: array
    description: "Type inverse exact match (case-insensitive)"
  type__niew:
    required: false
    type: array
    description: "Type does not end with (case-insensitive)"
  type__nisw:
    required: false
    type: array
    description: "Type does not start with (case-sensitive)"
  type__regex:
    required: false
    type: array
    description: "Type"
  unterminated:
    required: false
    type: boolean
    description: "Unterminated"
  updated_by_request:
    required: false
    type: string
    description: "Updated_by_request"
  save_in_key_store:
    type: boolean
    default: false
    description: Save the result of the action as a json object in the st2 key store. Used when the expected result from Netbox is very large and the result will be piped to another action. You must also specify a save_in_key_store_keyname and an optional save_in_key_store_ttl.
  save_in_key_store_key_name:
    type: string
    description: Name of the key to store the json result value in the st2 key store. Must be used with save_in_key_store and optionally save_in_key_store_ttl.
  save_in_key_store_ttl:
    type: integer
    default: 60
    description: TTL (seconds) of the saved json result in the st2 key store. Defaults to 60 seconds. Must be used with save_in_key_store and save_in_key_store_key_name.
runner_type: python-script
