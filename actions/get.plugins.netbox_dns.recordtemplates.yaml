# This action was auto generated from the NetBox API Swagger Spec
# NetBox API version: 4.3
description: "Get a list of Record Template objects."
enabled: true
entry_point: run.py
name: get.plugins.netbox_dns.recordtemplates
parameters:
  endpoint_uri:
    default: "/plugins/netbox-dns/recordtemplates/"
    immutable: true
    type: string
  http_verb:
    default: get
    immutable: true
    type: string
  get_detail_route_eligible:
    default: true
    immutable: true
    type: boolean
  created:
    required: false
    type: array
    description: "Created"
  created__empty:
    required: false
    type: array
    description: "Created is empty/null (boolean)"
  created__gt:
    required: false
    type: array
    description: "Created greater than"
  created__gte:
    required: false
    type: array
    description: "Created greater than or equal to"
  created__lt:
    required: false
    type: array
    description: "Created less than"
  created__lte:
    required: false
    type: array
    description: "Created less than or equal to"
  created__n:
    required: false
    type: array
    description: "Created not equal to"
  created_by_request:
    required: false
    type: string
    description: "Created_by_request"
  description:
    required: false
    type: array
    description: "Description"
  description__empty:
    required: false
    type: boolean
    description: "Description is empty/null (boolean)"
  description__ic:
    required: false
    type: array
    description: "Description contains (case-insensitive)"
  description__ie:
    required: false
    type: array
    description: "Description exact match (case-insensitive)"
  description__iew:
    required: false
    type: array
    description: "Description ends with (case-insensitive)"
  description__isw:
    required: false
    type: array
    description: "Description starts with (case-sensitive)"
  description__n:
    required: false
    type: array
    description: "Description not equal to"
  description__nic:
    required: false
    type: array
    description: "Description does not contain (case-insensitive)"
  description__nie:
    required: false
    type: array
    description: "Description inverse exact match (case-insensitive)"
  description__niew:
    required: false
    type: array
    description: "Description does not end with (case-insensitive)"
  description__nisw:
    required: false
    type: array
    description: "Description does not start with (case-sensitive)"
  disable_ptr:
    required: false
    type: boolean
    description: "Disable_ptr"
  id:
    required: false
    type: array
    description: "Id"
  id__empty:
    required: false
    type: boolean
    description: "Id is empty/null (boolean)"
  id__gt:
    required: false
    type: array
    description: "Id greater than"
  id__gte:
    required: false
    type: array
    description: "Id greater than or equal to"
  id__lt:
    required: false
    type: array
    description: "Id less than"
  id__lte:
    required: false
    type: array
    description: "Id less than or equal to"
  id__n:
    required: false
    type: array
    description: "Id not equal to"
  last_updated:
    required: false
    type: array
    description: "Last_updated"
  last_updated__empty:
    required: false
    type: array
    description: "Last_updated is empty/null (boolean)"
  last_updated__gt:
    required: false
    type: array
    description: "Last_updated greater than"
  last_updated__gte:
    required: false
    type: array
    description: "Last_updated greater than or equal to"
  last_updated__lt:
    required: false
    type: array
    description: "Last_updated less than"
  last_updated__lte:
    required: false
    type: array
    description: "Last_updated less than or equal to"
  last_updated__n:
    required: false
    type: array
    description: "Last_updated not equal to"
  limit:
    required: false
    type: integer
    description: "Number of results to return per page."
  modified_by_request:
    required: false
    type: string
    description: "Modified_by_request"
  name:
    required: false
    type: array
    description: "Name"
  name__empty:
    required: false
    type: boolean
    description: "Name is empty/null (boolean)"
  name__ic:
    required: false
    type: array
    description: "Name contains (case-insensitive)"
  name__ie:
    required: false
    type: array
    description: "Name exact match (case-insensitive)"
  name__iew:
    required: false
    type: array
    description: "Name ends with (case-insensitive)"
  name__isw:
    required: false
    type: array
    description: "Name starts with (case-sensitive)"
  name__n:
    required: false
    type: array
    description: "Name not equal to"
  name__nic:
    required: false
    type: array
    description: "Name does not contain (case-insensitive)"
  name__nie:
    required: false
    type: array
    description: "Name inverse exact match (case-insensitive)"
  name__niew:
    required: false
    type: array
    description: "Name does not end with (case-insensitive)"
  name__nisw:
    required: false
    type: array
    description: "Name does not start with (case-sensitive)"
  offset:
    required: false
    type: integer
    description: "The initial index from which to return the results."
  ordering:
    required: false
    type: string
    description: "Which field to use when ordering the results."
  q:
    required: false
    type: string
    description: "Search"
  record_name:
    required: false
    type: array
    description: "Record_name"
  record_name__empty:
    required: false
    type: boolean
    description: "Record_name is empty/null (boolean)"
  record_name__ic:
    required: false
    type: array
    description: "Record_name contains (case-insensitive)"
  record_name__ie:
    required: false
    type: array
    description: "Record_name exact match (case-insensitive)"
  record_name__iew:
    required: false
    type: array
    description: "Record_name ends with (case-insensitive)"
  record_name__isw:
    required: false
    type: array
    description: "Record_name starts with (case-sensitive)"
  record_name__n:
    required: false
    type: array
    description: "Record_name not equal to"
  record_name__nic:
    required: false
    type: array
    description: "Record_name does not contain (case-insensitive)"
  record_name__nie:
    required: false
    type: array
    description: "Record_name inverse exact match (case-insensitive)"
  record_name__niew:
    required: false
    type: array
    description: "Record_name does not end with (case-insensitive)"
  record_name__nisw:
    required: false
    type: array
    description: "Record_name does not start with (case-sensitive)"
  status:
    required: false
    type: array
    description: "Status"
  status__empty:
    required: false
    type: boolean
    description: "Status is empty/null (boolean)"
  status__ic:
    required: false
    type: array
    description: "Status contains (case-insensitive)"
  status__ie:
    required: false
    type: array
    description: "Status exact match (case-insensitive)"
  status__iew:
    required: false
    type: array
    description: "Status ends with (case-insensitive)"
  status__isw:
    required: false
    type: array
    description: "Status starts with (case-sensitive)"
  status__n:
    required: false
    type: array
    description: "Status not equal to"
  status__nic:
    required: false
    type: array
    description: "Status does not contain (case-insensitive)"
  status__nie:
    required: false
    type: array
    description: "Status inverse exact match (case-insensitive)"
  status__niew:
    required: false
    type: array
    description: "Status does not end with (case-insensitive)"
  status__nisw:
    required: false
    type: array
    description: "Status does not start with (case-sensitive)"
  tag:
    required: false
    type: array
    description: "Tag"
  tag__n:
    required: false
    type: array
    description: "Tag not equal to"
  tag_id:
    required: false
    type: array
    description: "Tag_id"
  tag_id__n:
    required: false
    type: array
    description: "Tag_id not equal to"
  tenant:
    required: false
    type: array
    description: "Tenant (slug)"
  tenant__n:
    required: false
    type: array
    description: "Tenant not equal to"
  tenant_group:
    required: false
    type: array
    description: "Tenant_group"
  tenant_group__n:
    required: false
    type: array
    description: "Tenant_group not equal to"
  tenant_group_id:
    required: false
    type: array
    description: "Tenant_group_id"
  tenant_group_id__n:
    required: false
    type: array
    description: "Tenant_group_id not equal to"
  tenant_id:
    required: false
    type: array
    description: "Tenant (ID)"
  tenant_id__n:
    required: false
    type: array
    description: "Tenant_id not equal to"
  ttl:
    required: false
    type: array
    description: "Ttl"
  ttl__empty:
    required: false
    type: boolean
    description: "Ttl is empty/null (boolean)"
  ttl__gt:
    required: false
    type: array
    description: "Ttl greater than"
  ttl__gte:
    required: false
    type: array
    description: "Ttl greater than or equal to"
  ttl__lt:
    required: false
    type: array
    description: "Ttl less than"
  ttl__lte:
    required: false
    type: array
    description: "Ttl less than or equal to"
  ttl__n:
    required: false
    type: array
    description: "Ttl not equal to"
  type:
    required: false
    type: array
    description: "Type"
  type__empty:
    required: false
    type: boolean
    description: "Type is empty/null (boolean)"
  type__ic:
    required: false
    type: array
    description: "Type contains (case-insensitive)"
  type__ie:
    required: false
    type: array
    description: "Type exact match (case-insensitive)"
  type__iew:
    required: false
    type: array
    description: "Type ends with (case-insensitive)"
  type__isw:
    required: false
    type: array
    description: "Type starts with (case-sensitive)"
  type__n:
    required: false
    type: array
    description: "Type not equal to"
  type__nic:
    required: false
    type: array
    description: "Type does not contain (case-insensitive)"
  type__nie:
    required: false
    type: array
    description: "Type inverse exact match (case-insensitive)"
  type__niew:
    required: false
    type: array
    description: "Type does not end with (case-insensitive)"
  type__nisw:
    required: false
    type: array
    description: "Type does not start with (case-sensitive)"
  updated_by_request:
    required: false
    type: string
    description: "Updated_by_request"
  value:
    required: false
    type: array
    description: "Value"
  value__empty:
    required: false
    type: boolean
    description: "Value is empty/null (boolean)"
  value__ic:
    required: false
    type: array
    description: "Value contains (case-insensitive)"
  value__ie:
    required: false
    type: array
    description: "Value exact match (case-insensitive)"
  value__iew:
    required: false
    type: array
    description: "Value ends with (case-insensitive)"
  value__isw:
    required: false
    type: array
    description: "Value starts with (case-sensitive)"
  value__n:
    required: false
    type: array
    description: "Value not equal to"
  value__nic:
    required: false
    type: array
    description: "Value does not contain (case-insensitive)"
  value__nie:
    required: false
    type: array
    description: "Value inverse exact match (case-insensitive)"
  value__niew:
    required: false
    type: array
    description: "Value does not end with (case-insensitive)"
  value__nisw:
    required: false
    type: array
    description: "Value does not start with (case-sensitive)"
  zone_template:
    required: false
    type: array
    description: "Zone Template"
  zone_template__n:
    required: false
    type: array
    description: "Zone_template not equal to"
  zone_template_id:
    required: false
    type: array
    description: "Zone Template ID"
  zone_template_id__n:
    required: false
    type: array
    description: "Zone_template_id not equal to"
  save_in_key_store:
    type: boolean
    default: false
    description: Save the result of the action as a json object in the st2 key store. Used when the expected result from Netbox is very large and the result will be piped to another action. You must also specify a save_in_key_store_keyname and an optional save_in_key_store_ttl.
  save_in_key_store_key_name:
    type: string
    description: Name of the key to store the json result value in the st2 key store. Must be used with save_in_key_store and optionally save_in_key_store_ttl.
  save_in_key_store_ttl:
    type: integer
    default: 60
    description: TTL (seconds) of the saved json result in the st2 key store. Defaults to 60 seconds. Must be used with save_in_key_store and save_in_key_store_key_name.
runner_type: python-script
