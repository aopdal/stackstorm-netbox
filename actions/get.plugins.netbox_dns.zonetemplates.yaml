# This action was auto generated from the NetBox API Swagger Spec
# NetBox API version: 4.3
description: "Get a list of Zone Template objects."
enabled: true
entry_point: run.py
name: get.plugins.netbox_dns.zonetemplates
parameters:
  endpoint_uri:
    default: "/plugins/netbox-dns/zonetemplates/"
    immutable: true
    type: string
  http_verb:
    default: get
    immutable: true
    type: string
  get_detail_route_eligible:
    default: true
    immutable: true
    type: boolean
  admin_c:
    required: false
    type: array
    description: "Administrative Contact"
  admin_c__n:
    required: false
    type: array
    description: "Admin_c not equal to"
  admin_c_id:
    required: false
    type: array
    description: "Administrative Contact ID"
  admin_c_id__n:
    required: false
    type: array
    description: "Admin_c_id not equal to"
  billing_c:
    required: false
    type: array
    description: "Billing Contact"
  billing_c__n:
    required: false
    type: array
    description: "Billing_c not equal to"
  billing_c_id:
    required: false
    type: array
    description: "Billing Contact ID"
  billing_c_id__n:
    required: false
    type: array
    description: "Billing_c_id not equal to"
  created:
    required: false
    type: array
    description: "Created"
  created__empty:
    required: false
    type: array
    description: "Created is empty/null (boolean)"
  created__gt:
    required: false
    type: array
    description: "Created greater than"
  created__gte:
    required: false
    type: array
    description: "Created greater than or equal to"
  created__lt:
    required: false
    type: array
    description: "Created less than"
  created__lte:
    required: false
    type: array
    description: "Created less than or equal to"
  created__n:
    required: false
    type: array
    description: "Created not equal to"
  created_by_request:
    required: false
    type: string
    description: "Created_by_request"
  description:
    required: false
    type: array
    description: "Description"
  description__empty:
    required: false
    type: boolean
    description: "Description is empty/null (boolean)"
  description__ic:
    required: false
    type: array
    description: "Description contains (case-insensitive)"
  description__ie:
    required: false
    type: array
    description: "Description exact match (case-insensitive)"
  description__iew:
    required: false
    type: array
    description: "Description ends with (case-insensitive)"
  description__isw:
    required: false
    type: array
    description: "Description starts with (case-sensitive)"
  description__n:
    required: false
    type: array
    description: "Description not equal to"
  description__nic:
    required: false
    type: array
    description: "Description does not contain (case-insensitive)"
  description__nie:
    required: false
    type: array
    description: "Description inverse exact match (case-insensitive)"
  description__niew:
    required: false
    type: array
    description: "Description does not end with (case-insensitive)"
  description__nisw:
    required: false
    type: array
    description: "Description does not start with (case-sensitive)"
  dnssec_policy:
    required: false
    type: array
    description: "DNSSEC Policy"
  dnssec_policy__n:
    required: false
    type: array
    description: "Dnssec_policy not equal to"
  dnssec_policy_id:
    required: false
    type: array
    description: "DNSSEC Policy ID"
  dnssec_policy_id__n:
    required: false
    type: array
    description: "Dnssec_policy_id not equal to"
  id:
    required: false
    type: array
    description: "Id"
  id__empty:
    required: false
    type: boolean
    description: "Id is empty/null (boolean)"
  id__gt:
    required: false
    type: array
    description: "Id greater than"
  id__gte:
    required: false
    type: array
    description: "Id greater than or equal to"
  id__lt:
    required: false
    type: array
    description: "Id less than"
  id__lte:
    required: false
    type: array
    description: "Id less than or equal to"
  id__n:
    required: false
    type: array
    description: "Id not equal to"
  last_updated:
    required: false
    type: array
    description: "Last_updated"
  last_updated__empty:
    required: false
    type: array
    description: "Last_updated is empty/null (boolean)"
  last_updated__gt:
    required: false
    type: array
    description: "Last_updated greater than"
  last_updated__gte:
    required: false
    type: array
    description: "Last_updated greater than or equal to"
  last_updated__lt:
    required: false
    type: array
    description: "Last_updated less than"
  last_updated__lte:
    required: false
    type: array
    description: "Last_updated less than or equal to"
  last_updated__n:
    required: false
    type: array
    description: "Last_updated not equal to"
  limit:
    required: false
    type: integer
    description: "Number of results to return per page."
  modified_by_request:
    required: false
    type: string
    description: "Modified_by_request"
  name:
    required: false
    type: array
    description: "Name"
  name__empty:
    required: false
    type: boolean
    description: "Name is empty/null (boolean)"
  name__ic:
    required: false
    type: array
    description: "Name contains (case-insensitive)"
  name__ie:
    required: false
    type: array
    description: "Name exact match (case-insensitive)"
  name__iew:
    required: false
    type: array
    description: "Name ends with (case-insensitive)"
  name__isw:
    required: false
    type: array
    description: "Name starts with (case-sensitive)"
  name__n:
    required: false
    type: array
    description: "Name not equal to"
  name__nic:
    required: false
    type: array
    description: "Name does not contain (case-insensitive)"
  name__nie:
    required: false
    type: array
    description: "Name inverse exact match (case-insensitive)"
  name__niew:
    required: false
    type: array
    description: "Name does not end with (case-insensitive)"
  name__nisw:
    required: false
    type: array
    description: "Name does not start with (case-sensitive)"
  nameserver:
    required: false
    type: array
    description: "Nameserver"
  nameserver__n:
    required: false
    type: array
    description: "Nameserver not equal to"
  nameserver_id:
    required: false
    type: array
    description: "Nameservers ID"
  nameserver_id__n:
    required: false
    type: array
    description: "Nameserver_id not equal to"
  offset:
    required: false
    type: integer
    description: "The initial index from which to return the results."
  ordering:
    required: false
    type: string
    description: "Which field to use when ordering the results."
  q:
    required: false
    type: string
    description: "Search"
  record_template:
    required: false
    type: array
    description: "Record Template"
  record_template__n:
    required: false
    type: array
    description: "Record_template not equal to"
  record_template_id:
    required: false
    type: array
    description: "Record Template ID"
  record_template_id__n:
    required: false
    type: array
    description: "Record_template_id not equal to"
  registrant:
    required: false
    type: array
    description: "Registrant"
  registrant__n:
    required: false
    type: array
    description: "Registrant not equal to"
  registrant_id:
    required: false
    type: array
    description: "Registrant ID"
  registrant_id__n:
    required: false
    type: array
    description: "Registrant_id not equal to"
  registrar:
    required: false
    type: array
    description: "Registrar"
  registrar__n:
    required: false
    type: array
    description: "Registrar not equal to"
  registrar_id:
    required: false
    type: array
    description: "Registrar ID"
  registrar_id__n:
    required: false
    type: array
    description: "Registrar_id not equal to"
  soa_mname:
    required: false
    type: array
    description: "SOA MName"
  soa_mname__n:
    required: false
    type: array
    description: "Soa_mname not equal to"
  soa_mname_id:
    required: false
    type: array
    description: "SOA MName ID"
  soa_mname_id__n:
    required: false
    type: array
    description: "Soa_mname_id not equal to"
  soa_rname:
    required: false
    type: array
    description: "Soa_rname"
  soa_rname__empty:
    required: false
    type: boolean
    description: "Soa_rname is empty/null (boolean)"
  soa_rname__ic:
    required: false
    type: array
    description: "Soa_rname contains (case-insensitive)"
  soa_rname__ie:
    required: false
    type: array
    description: "Soa_rname exact match (case-insensitive)"
  soa_rname__iew:
    required: false
    type: array
    description: "Soa_rname ends with (case-insensitive)"
  soa_rname__isw:
    required: false
    type: array
    description: "Soa_rname starts with (case-sensitive)"
  soa_rname__n:
    required: false
    type: array
    description: "Soa_rname not equal to"
  soa_rname__nic:
    required: false
    type: array
    description: "Soa_rname does not contain (case-insensitive)"
  soa_rname__nie:
    required: false
    type: array
    description: "Soa_rname inverse exact match (case-insensitive)"
  soa_rname__niew:
    required: false
    type: array
    description: "Soa_rname does not end with (case-insensitive)"
  soa_rname__nisw:
    required: false
    type: array
    description: "Soa_rname does not start with (case-sensitive)"
  tag:
    required: false
    type: array
    description: "Tag"
  tag__n:
    required: false
    type: array
    description: "Tag not equal to"
  tag_id:
    required: false
    type: array
    description: "Tag_id"
  tag_id__n:
    required: false
    type: array
    description: "Tag_id not equal to"
  tech_c:
    required: false
    type: array
    description: "Technical Contact"
  tech_c__n:
    required: false
    type: array
    description: "Tech_c not equal to"
  tech_c_id:
    required: false
    type: array
    description: "Technical Contact ID"
  tech_c_id__n:
    required: false
    type: array
    description: "Tech_c_id not equal to"
  tenant:
    required: false
    type: array
    description: "Tenant (slug)"
  tenant__n:
    required: false
    type: array
    description: "Tenant not equal to"
  tenant_group:
    required: false
    type: array
    description: "Tenant_group"
  tenant_group__n:
    required: false
    type: array
    description: "Tenant_group not equal to"
  tenant_group_id:
    required: false
    type: array
    description: "Tenant_group_id"
  tenant_group_id__n:
    required: false
    type: array
    description: "Tenant_group_id not equal to"
  tenant_id:
    required: false
    type: array
    description: "Tenant (ID)"
  tenant_id__n:
    required: false
    type: array
    description: "Tenant_id not equal to"
  updated_by_request:
    required: false
    type: string
    description: "Updated_by_request"
  save_in_key_store:
    type: boolean
    default: false
    description: Save the result of the action as a json object in the st2 key store. Used when the expected result from Netbox is very large and the result will be piped to another action. You must also specify a save_in_key_store_keyname and an optional save_in_key_store_ttl.
  save_in_key_store_key_name:
    type: string
    description: Name of the key to store the json result value in the st2 key store. Must be used with save_in_key_store and optionally save_in_key_store_ttl.
  save_in_key_store_ttl:
    type: integer
    default: 60
    description: TTL (seconds) of the saved json result in the st2 key store. Defaults to 60 seconds. Must be used with save_in_key_store and save_in_key_store_key_name.
runner_type: python-script
