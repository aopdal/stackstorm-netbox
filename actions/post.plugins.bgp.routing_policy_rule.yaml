# This action was auto generated from the NetBox API Swagger Spec
# NetBox API version: 4.6
description: "Post a list of routing policy rule objects."
enabled: true
entry_point: run.py
name: post.plugins.bgp.routing_policy_rule
parameters:
  endpoint_uri:
    default: "/plugins/bgp/routing-policy-rule/"
    immutable: true
    type: string
  http_verb:
    default: post
    immutable: true
    type: string
  get_detail_route_eligible:
    default: true
    immutable: true
    type: boolean
  fail_non_2xx:
    default: false
    type: boolean
    description: Set action as failed when http return reponse status isn't 2xx.
runner_type: python-script
