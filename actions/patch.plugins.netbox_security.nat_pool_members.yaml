# This action was auto generated from the NetBox API Swagger Spec
# NetBox API version: 4.5
description: "Patch a NAT Pool Member object."
enabled: true
entry_point: run.py
name: patch.plugins.netbox_security.nat_pool_members
parameters:
  endpoint_uri:
    default: "/plugins/netbox-security/nat-pool-members/{{ id }}/"
    immutable: true
    type: string
  http_verb:
    default: patch
    immutable: true
    type: string
  get_detail_route_eligible:
    default: true
    immutable: true
    type: boolean
  fail_non_2xx:
    default: false
    type: boolean
    description: Set action as failed when http return reponse status isn't 2xx.
  name:
    required: false
    type: string
    description: "Name"
  description:
    required: false
    type: string
    description: "Description"
  pool:
    required: false
    type: object
    description: "Pool"
  status:
    required: false
    type: string
    description: "The operational status of this NAT Pool Member

* `active` - Active
* `reserved` - Reserved
* `deprecated` - Deprecated
* `dhcp` - DHCP
* `slaac` - SLAAC"
  address:
    required: false
    type: object
    description: "Address"
  prefix:
    required: false
    type: object
    description: "Prefix"
  address_range:
    required: false
    type: object
    description: "Address range"
  source_ports:
    required: false
    type: array
    description: "Source Port numbers"
  destination_ports:
    required: false
    type: array
    description: "Destination Port numbers"
  tags:
    required: false
    type: array
    description: "Array of tag strings"
  custom_fields:
    required: false
    type: object
    description: "Custom fields"
  id:
    required: true
    type: integer
    description: "ID of the object to patch."
runner_type: python-script
