# This action was auto generated from the NetBox API Swagger Spec
# NetBox API version: 4.5
description: "Get a list of NAT Pool Member objects."
enabled: true
entry_point: run.py
name: get.plugins.netbox_security.nat_pool_members
parameters:
  endpoint_uri:
    default: "/plugins/netbox-security/nat-pool-members/"
    immutable: true
    type: string
  http_verb:
    default: get
    immutable: true
    type: string
  get_detail_route_eligible:
    default: true
    immutable: true
    type: boolean
  fail_non_2xx:
    default: false
    type: boolean
    description: Set action as failed when http return reponse status isn't 2xx.
  address:
    required: false
    type: array
    description: "Address"
  address__n:
    required: false
    type: array
    description: "Address not equal to"
  address_id:
    required: false
    type: array
    description: "Address (ID)"
  address_id__n:
    required: false
    type: array
    description: "Address_id not equal to"
  address_range:
    required: false
    type: array
    description: "IPRange (Start Address)"
  address_range__n:
    required: false
    type: array
    description: "Address_range not equal to"
  address_range_id:
    required: false
    type: array
    description: "IPRange (ID)"
  address_range_id__n:
    required: false
    type: array
    description: "Address_range_id not equal to"
  created:
    required: false
    type: array
    description: "Created"
  created__empty:
    required: false
    type: array
    description: "Created is empty/null (boolean)"
  created__gt:
    required: false
    type: array
    description: "Created greater than"
  created__gte:
    required: false
    type: array
    description: "Created greater than or equal to"
  created__lt:
    required: false
    type: array
    description: "Created less than"
  created__lte:
    required: false
    type: array
    description: "Created less than or equal to"
  created__n:
    required: false
    type: array
    description: "Created not equal to"
  created_by_request:
    required: false
    type: string
    description: "Created_by_request"
  description:
    required: false
    type: array
    description: "Description"
  description__empty:
    required: false
    type: boolean
    description: "Description is empty/null (boolean)"
  description__ic:
    required: false
    type: array
    description: "Description contains (case-insensitive)"
  description__ie:
    required: false
    type: array
    description: "Description exact match (case-insensitive)"
  description__iew:
    required: false
    type: array
    description: "Description ends with (case-insensitive)"
  description__iregex:
    required: false
    type: array
    description: "Description"
  description__isw:
    required: false
    type: array
    description: "Description starts with (case-sensitive)"
  description__n:
    required: false
    type: array
    description: "Description not equal to"
  description__nic:
    required: false
    type: array
    description: "Description does not contain (case-insensitive)"
  description__nie:
    required: false
    type: array
    description: "Description inverse exact match (case-insensitive)"
  description__niew:
    required: false
    type: array
    description: "Description does not end with (case-insensitive)"
  description__nisw:
    required: false
    type: array
    description: "Description does not start with (case-sensitive)"
  description__regex:
    required: false
    type: array
    description: "Description"
  destination_ports:
    required: false
    type: integer
    description: "Destination_ports"
  destination_ports__empty:
    required: false
    type: integer
    description: "Destination_ports is empty/null (boolean)"
  destination_ports__gt:
    required: false
    type: integer
    description: "Destination_ports greater than"
  destination_ports__gte:
    required: false
    type: integer
    description: "Destination_ports greater than or equal to"
  destination_ports__lt:
    required: false
    type: integer
    description: "Destination_ports less than"
  destination_ports__lte:
    required: false
    type: integer
    description: "Destination_ports less than or equal to"
  destination_ports__n:
    required: false
    type: integer
    description: "Destination_ports not equal to"
  id:
    required: false
    type: array
    description: "Id"
  id__empty:
    required: false
    type: boolean
    description: "Id is empty/null (boolean)"
  id__gt:
    required: false
    type: array
    description: "Id greater than"
  id__gte:
    required: false
    type: array
    description: "Id greater than or equal to"
  id__lt:
    required: false
    type: array
    description: "Id less than"
  id__lte:
    required: false
    type: array
    description: "Id less than or equal to"
  id__n:
    required: false
    type: array
    description: "Id not equal to"
  last_updated:
    required: false
    type: array
    description: "Last_updated"
  last_updated__empty:
    required: false
    type: array
    description: "Last_updated is empty/null (boolean)"
  last_updated__gt:
    required: false
    type: array
    description: "Last_updated greater than"
  last_updated__gte:
    required: false
    type: array
    description: "Last_updated greater than or equal to"
  last_updated__lt:
    required: false
    type: array
    description: "Last_updated less than"
  last_updated__lte:
    required: false
    type: array
    description: "Last_updated less than or equal to"
  last_updated__n:
    required: false
    type: array
    description: "Last_updated not equal to"
  limit:
    required: false
    type: integer
    description: "Number of results to return per page."
  modified_by_request:
    required: false
    type: string
    description: "Modified_by_request"
  name:
    required: false
    type: array
    description: "Name"
  name__empty:
    required: false
    type: boolean
    description: "Name is empty/null (boolean)"
  name__ic:
    required: false
    type: array
    description: "Name contains (case-insensitive)"
  name__ie:
    required: false
    type: array
    description: "Name exact match (case-insensitive)"
  name__iew:
    required: false
    type: array
    description: "Name ends with (case-insensitive)"
  name__iregex:
    required: false
    type: array
    description: "Name"
  name__isw:
    required: false
    type: array
    description: "Name starts with (case-sensitive)"
  name__n:
    required: false
    type: array
    description: "Name not equal to"
  name__nic:
    required: false
    type: array
    description: "Name does not contain (case-insensitive)"
  name__nie:
    required: false
    type: array
    description: "Name inverse exact match (case-insensitive)"
  name__niew:
    required: false
    type: array
    description: "Name does not end with (case-insensitive)"
  name__nisw:
    required: false
    type: array
    description: "Name does not start with (case-sensitive)"
  name__regex:
    required: false
    type: array
    description: "Name"
  offset:
    required: false
    type: integer
    description: "The initial index from which to return the results."
  ordering:
    required: false
    type: string
    description: "Which field to use when ordering the results."
  owner:
    required: false
    type: array
    description: "Owner (name)"
  owner__n:
    required: false
    type: array
    description: "Owner not equal to"
  owner_group:
    required: false
    type: array
    description: "Owner Group (name)"
  owner_group__n:
    required: false
    type: array
    description: "Owner_group not equal to"
  owner_group_id:
    required: false
    type: array
    description: "Owner Group (ID)"
  owner_group_id__n:
    required: false
    type: array
    description: "Owner_group_id not equal to"
  owner_id:
    required: false
    type: array
    description: "Owner (ID)"
  owner_id__n:
    required: false
    type: array
    description: "Owner_id not equal to"
  pool:
    required: false
    type: array
    description: "NAT Pool (Name)"
  pool__n:
    required: false
    type: array
    description: "Pool not equal to"
  pool_id:
    required: false
    type: array
    description: "NAT Pool (ID)"
  pool_id__n:
    required: false
    type: array
    description: "Pool_id not equal to"
  prefix:
    required: false
    type: array
    description: "Prefix (Prefix)"
  prefix__n:
    required: false
    type: array
    description: "Prefix not equal to"
  prefix_id:
    required: false
    type: array
    description: "Prefix (ID)"
  prefix_id__n:
    required: false
    type: array
    description: "Prefix_id not equal to"
  q:
    required: false
    type: string
    description: "Search"
  source_ports:
    required: false
    type: integer
    description: "Source_ports"
  source_ports__empty:
    required: false
    type: integer
    description: "Source_ports is empty/null (boolean)"
  source_ports__gt:
    required: false
    type: integer
    description: "Source_ports greater than"
  source_ports__gte:
    required: false
    type: integer
    description: "Source_ports greater than or equal to"
  source_ports__lt:
    required: false
    type: integer
    description: "Source_ports less than"
  source_ports__lte:
    required: false
    type: integer
    description: "Source_ports less than or equal to"
  source_ports__n:
    required: false
    type: integer
    description: "Source_ports not equal to"
  status:
    required: false
    type: array
    description: "The operational status of this NAT Pool Member"
  status__empty:
    required: false
    type: boolean
    description: "Status is empty/null (boolean)"
  status__ic:
    required: false
    type: array
    description: "Status contains (case-insensitive)"
  status__ie:
    required: false
    type: array
    description: "Status exact match (case-insensitive)"
  status__iew:
    required: false
    type: array
    description: "Status ends with (case-insensitive)"
  status__iregex:
    required: false
    type: array
    description: "The operational status of this NAT Pool Member"
  status__isw:
    required: false
    type: array
    description: "Status starts with (case-sensitive)"
  status__n:
    required: false
    type: array
    description: "Status not equal to"
  status__nic:
    required: false
    type: array
    description: "Status does not contain (case-insensitive)"
  status__nie:
    required: false
    type: array
    description: "Status inverse exact match (case-insensitive)"
  status__niew:
    required: false
    type: array
    description: "Status does not end with (case-insensitive)"
  status__nisw:
    required: false
    type: array
    description: "Status does not start with (case-sensitive)"
  status__regex:
    required: false
    type: array
    description: "The operational status of this NAT Pool Member"
  tag:
    required: false
    type: array
    description: "Tag"
  tag__n:
    required: false
    type: array
    description: "Tag not equal to"
  tag_id:
    required: false
    type: array
    description: "Tag_id"
  tag_id__n:
    required: false
    type: array
    description: "Tag_id not equal to"
  updated_by_request:
    required: false
    type: string
    description: "Updated_by_request"
  save_in_key_store:
    type: boolean
    default: false
    description: Save the result of the action as a json object in the st2 key store. Used when the expected result from Netbox is very large and the result will be piped to another action. You must also specify a save_in_key_store_keyname and an optional save_in_key_store_ttl.
  save_in_key_store_key_name:
    type: string
    description: Name of the key to store the json result value in the st2 key store. Must be used with save_in_key_store and optionally save_in_key_store_ttl.
  save_in_key_store_ttl:
    type: integer
    default: 60
    description: TTL (seconds) of the saved json result in the st2 key store. Defaults to 60 seconds. Must be used with save_in_key_store and save_in_key_store_key_name.
runner_type: python-script
