# This action was auto generated from the NetBox API Swagger Spec
# NetBox API version: 4.2
description: "Get a list of Registrar objects."
enabled: true
entry_point: run.py
name: get.plugins.netbox_dns.registrars
parameters:
  endpoint_uri:
    default: "/plugins/netbox-dns/registrars/"
    immutable: true
    type: string
  http_verb:
    default: get
    immutable: true
    type: string
  get_detail_route_eligible:
    default: true
    immutable: true
    type: boolean
  abuse_email:
    required: false
    type: array
    description: "Abuse_email"
  abuse_email__empty:
    required: false
    type: boolean
    description: "Abuse_email is empty/null (boolean)"
  abuse_email__ic:
    required: false
    type: array
    description: "Abuse_email contains (case-insensitive)"
  abuse_email__ie:
    required: false
    type: array
    description: "Abuse_email exact match (case-insensitive)"
  abuse_email__iew:
    required: false
    type: array
    description: "Abuse_email ends with (case-insensitive)"
  abuse_email__isw:
    required: false
    type: array
    description: "Abuse_email starts with (case-sensitive)"
  abuse_email__n:
    required: false
    type: array
    description: "Abuse_email not equal to"
  abuse_email__nic:
    required: false
    type: array
    description: "Abuse_email does not contain (case-insensitive)"
  abuse_email__nie:
    required: false
    type: array
    description: "Abuse_email inverse exact match (case-insensitive)"
  abuse_email__niew:
    required: false
    type: array
    description: "Abuse_email does not end with (case-insensitive)"
  abuse_email__nisw:
    required: false
    type: array
    description: "Abuse_email does not start with (case-sensitive)"
  abuse_phone:
    required: false
    type: array
    description: "Abuse_phone"
  abuse_phone__empty:
    required: false
    type: boolean
    description: "Abuse_phone is empty/null (boolean)"
  abuse_phone__ic:
    required: false
    type: array
    description: "Abuse_phone contains (case-insensitive)"
  abuse_phone__ie:
    required: false
    type: array
    description: "Abuse_phone exact match (case-insensitive)"
  abuse_phone__iew:
    required: false
    type: array
    description: "Abuse_phone ends with (case-insensitive)"
  abuse_phone__isw:
    required: false
    type: array
    description: "Abuse_phone starts with (case-sensitive)"
  abuse_phone__n:
    required: false
    type: array
    description: "Abuse_phone not equal to"
  abuse_phone__nic:
    required: false
    type: array
    description: "Abuse_phone does not contain (case-insensitive)"
  abuse_phone__nie:
    required: false
    type: array
    description: "Abuse_phone inverse exact match (case-insensitive)"
  abuse_phone__niew:
    required: false
    type: array
    description: "Abuse_phone does not end with (case-insensitive)"
  abuse_phone__nisw:
    required: false
    type: array
    description: "Abuse_phone does not start with (case-sensitive)"
  address:
    required: false
    type: array
    description: "Address"
  address__empty:
    required: false
    type: boolean
    description: "Address is empty/null (boolean)"
  address__ic:
    required: false
    type: array
    description: "Address contains (case-insensitive)"
  address__ie:
    required: false
    type: array
    description: "Address exact match (case-insensitive)"
  address__iew:
    required: false
    type: array
    description: "Address ends with (case-insensitive)"
  address__isw:
    required: false
    type: array
    description: "Address starts with (case-sensitive)"
  address__n:
    required: false
    type: array
    description: "Address not equal to"
  address__nic:
    required: false
    type: array
    description: "Address does not contain (case-insensitive)"
  address__nie:
    required: false
    type: array
    description: "Address inverse exact match (case-insensitive)"
  address__niew:
    required: false
    type: array
    description: "Address does not end with (case-insensitive)"
  address__nisw:
    required: false
    type: array
    description: "Address does not start with (case-sensitive)"
  created:
    required: false
    type: array
    description: "Created"
  created__empty:
    required: false
    type: array
    description: "Created is empty/null (boolean)"
  created__gt:
    required: false
    type: array
    description: "Created greater than"
  created__gte:
    required: false
    type: array
    description: "Created greater than or equal to"
  created__lt:
    required: false
    type: array
    description: "Created less than"
  created__lte:
    required: false
    type: array
    description: "Created less than or equal to"
  created__n:
    required: false
    type: array
    description: "Created not equal to"
  created_by_request:
    required: false
    type: string
    description: "Created_by_request"
  description:
    required: false
    type: array
    description: "Description"
  description__empty:
    required: false
    type: boolean
    description: "Description is empty/null (boolean)"
  description__ic:
    required: false
    type: array
    description: "Description contains (case-insensitive)"
  description__ie:
    required: false
    type: array
    description: "Description exact match (case-insensitive)"
  description__iew:
    required: false
    type: array
    description: "Description ends with (case-insensitive)"
  description__isw:
    required: false
    type: array
    description: "Description starts with (case-sensitive)"
  description__n:
    required: false
    type: array
    description: "Description not equal to"
  description__nic:
    required: false
    type: array
    description: "Description does not contain (case-insensitive)"
  description__nie:
    required: false
    type: array
    description: "Description inverse exact match (case-insensitive)"
  description__niew:
    required: false
    type: array
    description: "Description does not end with (case-insensitive)"
  description__nisw:
    required: false
    type: array
    description: "Description does not start with (case-sensitive)"
  iana_id:
    required: false
    type: array
    description: "Iana_id"
  iana_id__empty:
    required: false
    type: boolean
    description: "Iana_id is empty/null (boolean)"
  iana_id__gt:
    required: false
    type: array
    description: "Iana_id greater than"
  iana_id__gte:
    required: false
    type: array
    description: "Iana_id greater than or equal to"
  iana_id__lt:
    required: false
    type: array
    description: "Iana_id less than"
  iana_id__lte:
    required: false
    type: array
    description: "Iana_id less than or equal to"
  iana_id__n:
    required: false
    type: array
    description: "Iana_id not equal to"
  id:
    required: false
    type: array
    description: "Id"
  id__empty:
    required: false
    type: boolean
    description: "Id is empty/null (boolean)"
  id__gt:
    required: false
    type: array
    description: "Id greater than"
  id__gte:
    required: false
    type: array
    description: "Id greater than or equal to"
  id__lt:
    required: false
    type: array
    description: "Id less than"
  id__lte:
    required: false
    type: array
    description: "Id less than or equal to"
  id__n:
    required: false
    type: array
    description: "Id not equal to"
  last_updated:
    required: false
    type: array
    description: "Last_updated"
  last_updated__empty:
    required: false
    type: array
    description: "Last_updated is empty/null (boolean)"
  last_updated__gt:
    required: false
    type: array
    description: "Last_updated greater than"
  last_updated__gte:
    required: false
    type: array
    description: "Last_updated greater than or equal to"
  last_updated__lt:
    required: false
    type: array
    description: "Last_updated less than"
  last_updated__lte:
    required: false
    type: array
    description: "Last_updated less than or equal to"
  last_updated__n:
    required: false
    type: array
    description: "Last_updated not equal to"
  limit:
    required: false
    type: integer
    description: "Number of results to return per page."
  modified_by_request:
    required: false
    type: string
    description: "Modified_by_request"
  name:
    required: false
    type: array
    description: "Name"
  name__empty:
    required: false
    type: boolean
    description: "Name is empty/null (boolean)"
  name__ic:
    required: false
    type: array
    description: "Name contains (case-insensitive)"
  name__ie:
    required: false
    type: array
    description: "Name exact match (case-insensitive)"
  name__iew:
    required: false
    type: array
    description: "Name ends with (case-insensitive)"
  name__isw:
    required: false
    type: array
    description: "Name starts with (case-sensitive)"
  name__n:
    required: false
    type: array
    description: "Name not equal to"
  name__nic:
    required: false
    type: array
    description: "Name does not contain (case-insensitive)"
  name__nie:
    required: false
    type: array
    description: "Name inverse exact match (case-insensitive)"
  name__niew:
    required: false
    type: array
    description: "Name does not end with (case-insensitive)"
  name__nisw:
    required: false
    type: array
    description: "Name does not start with (case-sensitive)"
  offset:
    required: false
    type: integer
    description: "The initial index from which to return the results."
  ordering:
    required: false
    type: string
    description: "Which field to use when ordering the results."
  q:
    required: false
    type: string
    description: "Search"
  referral_url:
    required: false
    type: array
    description: "Referral_url"
  referral_url__empty:
    required: false
    type: boolean
    description: "Referral_url is empty/null (boolean)"
  referral_url__ic:
    required: false
    type: array
    description: "Referral_url contains (case-insensitive)"
  referral_url__ie:
    required: false
    type: array
    description: "Referral_url exact match (case-insensitive)"
  referral_url__iew:
    required: false
    type: array
    description: "Referral_url ends with (case-insensitive)"
  referral_url__isw:
    required: false
    type: array
    description: "Referral_url starts with (case-sensitive)"
  referral_url__n:
    required: false
    type: array
    description: "Referral_url not equal to"
  referral_url__nic:
    required: false
    type: array
    description: "Referral_url does not contain (case-insensitive)"
  referral_url__nie:
    required: false
    type: array
    description: "Referral_url inverse exact match (case-insensitive)"
  referral_url__niew:
    required: false
    type: array
    description: "Referral_url does not end with (case-insensitive)"
  referral_url__nisw:
    required: false
    type: array
    description: "Referral_url does not start with (case-sensitive)"
  tag:
    required: false
    type: array
    description: "Tag"
  tag__n:
    required: false
    type: array
    description: "Tag not equal to"
  updated_by_request:
    required: false
    type: string
    description: "Updated_by_request"
  whois_server:
    required: false
    type: array
    description: "Whois_server"
  whois_server__empty:
    required: false
    type: boolean
    description: "Whois_server is empty/null (boolean)"
  whois_server__ic:
    required: false
    type: array
    description: "Whois_server contains (case-insensitive)"
  whois_server__ie:
    required: false
    type: array
    description: "Whois_server exact match (case-insensitive)"
  whois_server__iew:
    required: false
    type: array
    description: "Whois_server ends with (case-insensitive)"
  whois_server__isw:
    required: false
    type: array
    description: "Whois_server starts with (case-sensitive)"
  whois_server__n:
    required: false
    type: array
    description: "Whois_server not equal to"
  whois_server__nic:
    required: false
    type: array
    description: "Whois_server does not contain (case-insensitive)"
  whois_server__nie:
    required: false
    type: array
    description: "Whois_server inverse exact match (case-insensitive)"
  whois_server__niew:
    required: false
    type: array
    description: "Whois_server does not end with (case-insensitive)"
  whois_server__nisw:
    required: false
    type: array
    description: "Whois_server does not start with (case-sensitive)"
  save_in_key_store:
    type: boolean
    default: false
    description: Save the result of the action as a json object in the st2 key store. Used when the expected result from Netbox is very large and the result will be piped to another action. You must also specify a save_in_key_store_keyname and an optional save_in_key_store_ttl.
  save_in_key_store_key_name:
    type: string
    description: Name of the key to store the json result value in the st2 key store. Must be used with save_in_key_store and optionally save_in_key_store_ttl.
  save_in_key_store_ttl:
    type: integer
    default: 60
    description: TTL (seconds) of the saved json result in the st2 key store. Defaults to 60 seconds. Must be used with save_in_key_store and save_in_key_store_key_name.
runner_type: python-script
